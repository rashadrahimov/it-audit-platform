---
name: verify
description: Как собрать, запустить и руками проверить IT Audit Platform (монорепо pnpm, api NestJS + web Next.js). Использовать при verify любого изменения.
---

# Verify: IT Audit Platform

Поверхность продукта — HTTP: API на :3001, веб на :3000. Проверяем запуском собранных артефактов, не тестами.

## Сборка и запуск

**Одна команда (дев): `pnpm dev:up`** — инфраструктура (compose + ожидание healthy) → build → seed → оба dev-сервера (api :3001, web :3000).

По шагам / для проверки собранных артефактов:

```bash
pnpm infra:up         # docker compose up -d + scripts/wait-infra.mjs (не compose --wait: он спотыкается об one-shot minio-init)
                      # Postgres :5433, Redis :6380, MinIO :9000 (консоль :9001), Mailpit :1025 (UI :8025),
                      # Keycloak :8081 (админка admin/admin; realm it-audit автоимпортируется, стартует ~30с),
                      # LDAP :1389 (osixia/openldap; base dc=demo,dc=io, админ cn=admin,dc=demo,dc=io/admin)
pnpm install          # если менялись зависимости
pnpm build            # shared → api (nest build) → web (next build); порядок топологический
pnpm db:migrate       # drizzle-kit: применяет миграции из apps/api/drizzle (схема — src/db/schema.ts)
pnpm db:migrate:down  # откат последней миграции (парные drizzle/down/<tag>.down.sql + раннер dist/db/migrate-down.js)
pnpm seed             # идемпотентный (apps/api/src/seed.ts): бакет audit-files + demo/welcome.txt;
                      # доменные данные: tenant «demo» (Demo Group) с дочкой demo-bank

# Продакшн-запуск собранных артефактов:
# API (из apps/api): node dist/main.js — порт из API_PORT, дефолт 3001
# Веб (из apps/web): npx next start — порт 3000; ждать "Ready in ..."
```

## Что дёргать

- `curl http://localhost:3001/health` — JSON `{status:"ok",service:"api",...}` по схеме из `packages/shared`.
- `curl http://localhost:3001/health/infra` — все четыре сервиса `ok:true`; если что-то лежит — HTTP 503, `status:"degraded"` и `error` у виновника. Письма смотреть в Mailpit UI `http://localhost:8025`, файлы — в MinIO-консоли `http://localhost:9001` (minioadmin/minioadmin).
- `http://localhost:3001/docs` — Swagger UI; `/docs-json` — OpenAPI-спека (должна содержать новые маршруты).
- `curl http://localhost:3000/` — главная; в HTML есть `data-testid="api-status"`: зелёный «api v0.0.1 — ok» при живом API, красный «API unavailable» (en) при погашенном (страница не 500-ит). Локаль — cookie: `curl -H 'Cookie: locale=ru'` → `<html lang="ru">` и русские строки (аналогично az); без/с мусорной cookie — en.

## Гочи

- Веб фетчит API **server-side** (адрес из `API_URL`), CORS не нужен; проверять именно HTML веба, а не только API.
- После правок кода перед `next start` / `node dist/main.js` нужен свежий `pnpm build` — серверы отдают собранное.
- `.env` не требуется: дефолты (3001, `http://localhost:3001`, `postgres://…:5433`, `redis://…:6380`, MinIO/SMTP) зашиты в код и совпадают с docker-compose.
- Postgres/Redis на **нестандартных хост-портах 5433/6380** — 5432/6379 заняты соседним проектом (leaddrive-uxtest). Внутри контейнеров порты стандартные.
- Порты 3000/3001 заняты? Тестовые процессы прошлого прогона: `ss -tlnp | grep ':300'` и убить.

- Seed сработал? В MinIO-консоли (или через S3 API) в бакете `audit-files` лежит `demo/welcome.txt` со свежим timestamp.
- БД (T-010): `docker exec it-audit-platform-postgres-1 psql -U audit -d audit -c '\dt public.*'` — таблицы `tenant`, `subsidiary`; после seed в них demo-строки. Откат: `pnpm db:migrate:down` убирает таблицы и запись журнала; `pnpm db:migrate` возвращает. Новая миграция = `pnpm db:generate` + обязательный парный `apps/api/drizzle/down/<tag>.down.sql`.
- RLS (T-011): `cd apps/api && npx vitest run test/rls.spec.ts` — 4 теста изоляции (нужны инфраструктура + миграции). Рантайм ходит под ролью `app` (не суперюзер!): доступ к доменным таблицам — только через `DbService.withTenant()`; без контекста — 0 строк. Новые доменные таблицы обязаны получать RLS-политику в той же миграции.
- Auth (T-013): `POST /auth/register` (слабый пароль → 400 со списком нарушений; `tenantSlug` применяет политику из `tenant.settings.passwordPolicy`), `POST /auth/login` → `{accessToken}`, `GET /auth/me` c Bearer (без — 401), `POST /auth/change-password` (204; старый пароль перестаёт работать). 5 неверных паролей подряд → lockout на 15 мин даже для верного. Хеш в БД начинается с `scrypt:`.
- RBAC (T-018/T-019): под Bearer — `GET /rbac/permissions` (30 шт. из seed), `GET /rbac/roles` — 7 системных пресетов (Admin…Approver; Approver: approve=edit, остальное view), `?tenantSlug=demo` добавляет роль «Демо-аудиторы» (итого 8); без `tenantSlug` тенантские роли не видны (RLS).
- Enforcement (T-020): демо-логины `admin@demo.io`/`Demo-Admin-2026` и `collaborator@demo.io`/`Demo-Collab-2026`. `POST /rbac/demo-edit` (+`X-Tenant-Slug: demo`): Collaborator → 403, Admin → 201, без заголовка → 400. `GET /rbac/check?resource=&action=` → `{level, canView, canEdit}` — этим UI прячет недоступное. Тест: `npx vitest run test/rbac-enforcement.spec.ts` (нужен прогнанный seed).
- Деактивация (T-017): `POST /users/:id/deactivate` (Admin, 204) → логин юзера 401; `.../reactivate` возвращает. Авто: суточная джоба `deactivate-inactive-users` (порог `INACTIVITY_DEACTIVATION_DAYS`, 90 дн.); тест `npx vitest run test/deactivation.spec.ts`.
- Лицензии (T-026): `GET /license/usage` (Admin+`X-Tenant-Slug: demo`) — план demo, дочки used/max 2/2, seats 2/5; Collaborator → 403. `POST /subsidiaries` сверх квоты — создаёт И возвращает warning (мягкая проверка ADR-0014); созданных сверх демо-набора дочек потом подчистить.
- Журналы (T-021): после логина — строка в `auth_event` (event/ip/user_agent), после `POST /subsidiaries` — в `audit_log` (action `subsidiary.created`, actor, after). Append-only: `psql -U app -d audit -c "UPDATE audit_log SET action='x'"` → permission denied.
- Soft-delete/comments (T-023): `DELETE /subsidiaries/:id` → 204, usage перестаёт считать удалённую; `POST /subsidiaries/:id/restore` возвращает (повторный — 404). `POST /comments` `{entityType, entityId, body}` → `GET /comments?entityType=&entityId=`; всё пишется в audit_log.
- Invite (T-015): `POST /invites` (Admin) `{email, roleId, isAuditSeat, locale}` → письмо в Mailpit с `token=` в тексте; до accept логин 401; `POST /invites/accept` `{token, password}` (слабый — 400) → 204 → логин приглашённого работает, seat считается в usage; в audit_log — `membership.granted` (LOG-05) и `invite.accepted`.
- OIDC SSO (T-016): `curl -i http://localhost:3001/auth/oidc/login` → 302 в Keycloak; полный код-флоу без браузера: страница логина → распарсить `action=` формы → POST `username=sso-user&password=Sso-User-2026` с cookie-jar → 302 на callback с кодом → `GET callback` отдаёт наш `{accessToken}`; `/auth/me` = sso-user@demo.io; повторный код → 401; JIT-юзер в БД с `password_hash IS NULL`.
- SAML SSO (T-024): готовый сценарий `bash <scratchpad>/saml-e2e.sh` или руками: `GET /auth/saml/login` → 302 на `.../protocol/saml?SAMLRequest=`; логин sso-user (те же креды, cookie-jar) → HTML с авто-submit формой → выдернуть `name="SAMLResponse" value="..."` → `POST /auth/saml/callback` (`--data-urlencode SAMLResponse=...`) → `{accessToken}`, `/auth/me` = sso-user@demo.io; **повторный SAMLResponse → 401** (InResponseTo-кэш, он in-memory — replay проверять на том же процессе api). Конфиг IdP api тянет из метадаты `:8081/realms/it-audit/protocol/saml/descriptor` при первом обращении. ⚠ Правки realm-json Keycloak применяются только пересозданием контейнера: `docker compose up -d --force-recreate keycloak` (dev-режим, H2 внутри контейнера — импорт заново) + подождать healthy ~30с.
- Frameworks (T-030): `GET /frameworks` (Bearer) — 3 глобальных из seed (ISO/IEC 27001 2022, COBIT 2019, NIST CSF 2.0, published; `?locale=`, `?tenantSlug=` добавит тенантские адаптации); без Bearer → 401. Requirements в БД: `framework_requirement` (ISO — 3, COBIT/NIST — по 2). RLS как у role: `psql -U app` без контекста видит только tenant_id NULL. UI: /frameworks за логином (cookie) — таблица `data-testid="frameworks-table"` с тремя стандартами, локализуется cookie locale; без сессии → 307 /login. Откат: `pnpm db:migrate:down` = 0009.
- Controls (T-031): `GET /controls` (Bearer; ?locale, ?tenantSlug) — 31 контроль из шаблона клиента (16 доменов; сид-данные `apps/api/src/seed-data/global-controls.ts` — сгенерированы из xlsx, руками не править); у GOV-01 standards = ISO/IEC 27001 A.5.1 + COBIT EDM01, у AM-01 — NIST CSF ID.AM (демо-маппинги, полные — EP-FWK). UI: /controls за логином — таблица `data-testid="controls-table"`, бейджи стандартов (в HTML между текстами React вставляет `<!-- -->` — при grep убирать). Откат: 0010.
- Control detail (T-032): `GET /controls/:id?tenantSlug=demo` — карточка одним ответом (owner, standards — у адаптации наследуются от оригинала, history из audit_log с актором, comments); `GET /auth/me/tenants` (Bearer) → `[{slug:"demo",name,role}]` — активный тенант UI (первый membership). Сид: тенантская адаптация GOV-01 в demo (owner admin, история control.adapted, коммент «CBAR checklist»); в списке `?tenantSlug=demo` — 32 строки (GOV-01 дважды: global+adapted). UI: /controls/<id> за логином — все блоки; id адаптации брать из API. ⚠ Грепать надо конкретные ЗНАЧЕНИЯ (Demo Admin…), не ключи переводов: словарь next-intl сериализован в HTML каждой страницы — «Tenant adaptation» найдётся везде.
- Engagement (T-035): всё под Bearer+`X-Tenant-Slug: demo` (PermissionGuard: Collaborator create→403, view→200). `POST /engagements` `{subsidiaryId, titleI18n, auditTypeCode: "it", mode: formal|light, milestones:[{stage, plannedDate}]}` → draft; `POST /engagements/:id/transition {to}`: formal — только следующая стадия (скип→400), light — скип разрешён только через согласовательные (manager_review/management_response/approval); пауза из любого рабочего, resume только в pausedFromState, archived только из closed (ставит archivedAt). Вход в стадию проставляет actual_date вехи (нет вехи — создаёт). `GET /engagements/:id` — allowedTransitions[] + вехи план/факт. Всё в audit_log (engagement.created/state_changed). UI: /engagements и /engagements/<id> (кнопки переходов — server actions; вехи план/факт таблицей).
- Чеклист (T-036): `POST /engagements/:id/checklist-items {controlIds:[...]}` (Bearer+X-Tenant-Slug, engagement.edit; Collaborator → 403) → `{added:n}`, повтор тех же → `{added:0}`; пункты — СНАПШОТЫ (objective/question скопированы в checklist_item — правка библиотеки их не меняет); `GET /engagements/:id` содержит `checklist[]` (локализованный). UI: секция «Чеклист» в карточке engagement — таблица + форма добавления (чекбоксы библиотеки без уже включённых). Откат: 0012.
- Ответы (T-037): `PUT /engagements/:id/checklist-items/:itemId/response {text, complianceStatus}` (Bearer+X-Tenant-Slug; под engagement.view — респонденты это Collaborator'ы) — upsert: повторный PUT перезаписывает (в БД одна строка на пункт), невалидный статус (`compliant|partially_compliant|non_compliant|not_applicable`) → 400; пункт получает `status=answered`, ответ виден в `checklist[].response` карточки (respondent, text, compliance). audit_log: `response.submitted`. UI: форма «Ответить на пункт» в карточке engagement (select пункта + textarea + select статуса), ответ рендерится под вопросом. Откат: 0013.
- Onboarding (T-046): `GET /onboarding` (Bearer+X-Tenant-Slug, settings.view: Collaborator → 403) — `{steps:[{key,done}], done, total}`, шаги ВЫЧИСЛЯЮТСЯ из данных (6: invite_team ≥2 членов, create_subsidiary, adapt_control, create_engagement, build_checklist, collect_responses); demo — 6/6, свежий пустой тенант — 0/6. UI: виджет с прогресс-баром на /account (`data-testid="onboarding"`); у не-админа секции нет.
- Документы (T-034): `POST /documents` (Bearer+X-Tenant-Slug, multipart `file` + опц. `entityType/entityId/relation/renewBy` — сразу привязка) → метаданные с sha256; `POST /documents/:id/links` — привязка (entityType из белого списка response/checklist_item/engagement/control/framework/subsidiary, relation evidence|permanent_file|attachment|report; мусор → 400; повтор тройки → `{linked:false}`); `GET /documents?entityType=&entityId=` — документы сущности; `GET /documents/:id/content` — авторизованное скачивание (сравнить `cmp`). audit_log: document.uploaded/linked. Откат: 0014.
- Tests (T-033): `POST /tests {controlId, titleI18n, frequency?, dueDate?}` (Bearer+X-Tenant-Slug, control.edit: Collaborator create → 403, list → 200) → статус `needs_attention`; `POST /tests/:id/results {outcome: pass|fail|error, failingEntities?, evidenceDocumentId?}` — outcome двигает статус (pass→ok, fail→failing, error→needs_attention), история прогонов в `GET /tests/:id`; `GET /tests?controlId=`. sla_status статичен до T-043. audit_log: test.created/result_recorded. UI: секция «Тесты» в /controls/<id> (`data-testid="control-tests"`, зелёный/красный бейдж). Откат: 0015.
- Findings (T-038): `POST /findings` (Bearer+X-Tenant-Slug, finding.create — у Collaborator есть) — все колонки чеклиста клиента: titleI18n, riskRating (`critical|high|medium|low|not_applicable`, мусор → 400), recommendation, owner (auditee) + auditor (audit-side) membership'ы, dueDate, managementResponse, привязки engagement/checklist_item/response/control (битая → 400); статус identified, sla=ok (до T-043). `GET /findings?engagementId=` — с owner/auditor именами; `GET /findings/:id` — все поля. audit_log: finding.created. UI: секция «Findings» в карточке engagement (`data-testid="engagement-findings"`, красный бейдж high/critical). Откат: 0016.
- SLA (T-043): `POST /jobs/sla-recalc` (Bearer, без — 401) → `{findings, tests}`; правило: due_date < now → `overdue`, < now+SLA_DUE_SOON_DAYS (7) → `due_soon`, иначе `ok`; закрытые finding и деактивированные test не трогаются. Планово: repeatable `sla-recalc-scheduler` раз в час (ключи `bull:system:repeat:sla*` в Redis), первый прогон при старте api. Проверка: finding с due_date вчера → overdue, test с завтра → due_soon.
- Finding lifecycle (T-039): `POST /findings/:id/assign {ownerMembershipId}` → assigned + письмо `finding-assigned` владельцу (Mailpit, локаль юзера); `POST /:id/transition {to}` — строго следующий шаг (скип → 400), remediated ставит remediated_at; **в formal-режиме `{to:"closed"}` → 400** («только через re-test»); `POST /:id/retest {result: passed|failed}` из pending_retest: passed → closed (+resolution_date/retested_by), failed → in_progress. Напоминания: `POST /jobs/finding-reminders` (Bearer) — письма owner+auditor для findings с due_date в окне SLA_DUE_SOON_DAYS, дедуп по reminder_sent_at (повтор → `{sent:0}`); планово раз в сутки. Откат: 0017.
- Автотесты через коннектор (T-050, continuous-loop): `POST /tests` с `kind:"automated", connectorId, checkConfig:{type:"field_present",field:"email"}` → автотест; `POST /tests/:id/run-auto` (control.edit) — коннектор→записи→правило→test_result (pass/fail + failing_entities)→статус теста. Демо: автотест «All LDAP accounts have email» на демо-коннекторе. **Живая демонстрация continuous compliance**: `ldapadd` аккаунта без email в openldap → run-auto → `outcome:fail, failing:[no-email]`, статус теста `failing`; `ldapdelete` → снова `pass/ok`. Repeatable-джоба `auto-test-run` раз в час (runAll по всем тенантам, actor=null/система). Сбой коннектора → тест `needs_attention` (error), не падение. UI `/connectors` за логином: список коннекторов (провайдер, capabilities, статус-бейдж, кнопка «Синхронизировать» — server action). Откат: 0021.
- Sync коннектора (T-049): `POST /connectors/:id/sync` (settings.edit, Collaborator → 403) — выбирает провайдера по `provider`, пишет sync_run. Демо-LDAP-коннектор в seed (на тестовый openldap): sync → `{outcome:"success", stats:{accounts:1, withEmail:1}}`, last_sync_at обновлён, история в GET-карточке. Битый конфиг (неверный bindPassword) → `{outcome:"error"}` + коннектор status=error, но **платформа не падает** (health 200, ADR-0011). audit_log connector.synced. Первый провайдер — `LdapConnectorProvider` (ldapts, capability personnel/access); реестр в ConnectorSyncService. Демо-коннектор нужен для T-050.
- Policy attestation (T-053): `POST /policies/:id/attestations {membershipIds}` (settings.edit) — только approved-политика (draft → 400); создаёт записи + письма `policy-attestation` (Mailpit). `POST /policies/:id/attest` (control.view — self-action, Collaborator может; не адресат → 404). `GET /policies/:id/attestations` — трекинг `{attested, total, rows}`. UI `/policies` — таблица статусов (`data-testid="policies-table"`). **EP-POL закрыт.** Откат: 0023.
- Policy workflow (T-052): `POST /policies/:id/submit {action:submit|archive}` (settings.edit) — draft→in_review, →archived; `POST /policies/:id/decision {action:approve|reject}` (settings.view — но реально только назначенный approver: workflow-роль ортогональна RBAC, проверка approver_membership в сервисе). Демо-approver `approver@demo.io`/`Demo-Approver-2026` (роль Approver, settings.view). E2e: submit→in_review, admin (не approver) approve→403, approver approve→approved (фиксирует approved_at/by на последней версии), archive→archived, недопустимый переход→400. audit_log policy.status_changed. UI /policies — с T-053.
- Аккаунты (T-054, EP-IAM): `POST /accounts/import {connectorId}` (settings.edit) — тянет personnel-records из коннектора (EP-INT) в `account` (upsert по tenant+connector+identifier, повтор не дублирует); `POST /accounts` ручной (identifier, type human/service, mfaEnabled); `GET /accounts` список (fromConnector-флаг); `POST /accounts/:id/deactivate` → status deactivated. Демо: импорт из демо-LDAP-коннектора → ldap-user. RBAC Collaborator → 403. audit_log account.imported/created/deactivated. UI /accounts — с T-055 (UAR). Откат: 0024.
- Политики (T-051, EP-POL): `POST /policies` (settings.edit) `{titleI18n, ownerMembershipId, approverMembershipId, frameworkIds, renewBy}` → status draft; `POST /policies/:id/versions {documentId (из T-034), changelog}` — версия с авто-инкрементом номера (v1, v2…); `GET /policies` список (owner-имя, статус), `GET /policies/:id` карточка+версии. RBAC Collaborator (settings=none) → 403. audit_log policy.created/version_added. Workflow (draft→approved) — T-052, attestation — T-053, UI — с T-052. Откат: 0022.
- Коннекторы (T-048, EP-INT): `POST /connectors` (Bearer+X-Tenant-Slug, settings.edit: Collaborator → 403) `{provider, capabilities:[access|inventory|personnel|evidence|vulns|tasks], config:{…креды}}` — конфиг шифруется AES-256-GCM (ENCRYPTION_KEY); мусорный capability → 400. Секреты наружу НЕ отдаются: GET список/карточка возвращают `hasConfig` (bool), не config; в БД `config_encrypted` не содержит plaintext (проверка `config_encrypted LIKE '%admin%'` → f); round-trip: `node -e '...decryptConfig...'` восстанавливает пароль. `GET /connectors/:id` — карточка + история sync_run (пока пустая, наполнит T-049). `DELETE` — soft. audit_log connector.created/deleted (без кредов). Откат: 0020.
- Экспорт отчёта (T-045): `GET /engagements/:id/report?format=pdf|docx|xlsx|csv|xml&locale=` (Bearer+X-Tenant-Slug, report.export=edit: Collaborator → 403; невалидный формат → 400) — стрим файла (шапка + чеклист-с-ответами + findings). Проверка контейнеров: PDF `%PDF`+встроен `DejaVuSans`+`%%EOF`; docx/xlsx — `PK` zip (`unzip`, кириллица в word/document.xml и xl/sharedStrings.xml, xlsx = 3 листа Summary/Checklist/Findings); csv/xml — кириллица напрямую. Шрифт DejaVu едет в репо (`apps/api/assets/fonts/`, nest-cli копирует в dist/assets) — стандартные PDF-шрифты кириллицу/AZ-латиницу не покрывают. UI: ряд кнопок форматов в карточке engagement (`data-testid="export-<fmt>"`) → Next route-handler `/engagements/[id]/report` проксирует cookie→Bearer (обычная `<a href>` auth-header не шлёт); без сессии → 307 /login.
- Дашборды (T-044): три уровня на `/group/summary` (T-012 расширен `departments[]`: finding→департамент через owner-membership.department_id). UI `/dashboard` за логином — KPI-карточки группы (`data-testid="dashboard-kpi"`), таблица по дочкам (`dashboard-subsidiaries`), по департаментам (`dashboard-departments`); без сессии → 307 /login. Демо-оргструктура в seed: Internal Audit (уровень группы), IT Department (демо-банк); admin→Internal Audit, collaborator→IT. Департамент виден в дашборде по тем же скоуп-правилам, что T-012. Откат: 0019.
- Группа (T-012): `GET /group/summary` (Bearer+X-Tenant-Slug, report.view) — `{scope, group:{engagementsByState, findingsByRating/Status, openFindings}, subsidiaries:[{name, …counts}]}`; findings мапятся на дочку через engagement, standalone — уровень группы. Скоуп: `UPDATE membership SET subsidiary_scope='["<id>"]'::jsonb` → юзер видит только эти дочки (`scope=restricted`), standalone-findings скрыты; NULL = вся группа. После проверки скоуп вернуть в NULL. Откат: 0018.
- UI-логин (T-047): готовый сценарий `node <scratchpad>/ui-login-e2e.mjs` (нужны api+web) — no-JS submit server actions: из HTML формы взять **все** hidden `$ACTION_*`-поля (у `$ACTION_REF_N` нет value — не потерять!) + поля формы, POST multipart на URL страницы; успех = 303 + `Set-Cookie session=` (httpOnly). Проверяет: логин admin@demo.io, /account (email юзера, `data-testid="account-*"`), guard без cookie → redirect /login, неверный пароль → `data-testid="login-error"`, MFA-юзер → второй шаг (`mfa-code`, hidden mfaToken) → TOTP → 303, logout → cookie очищена. Тест-юзера `mfa-ui-*@demo.io` после прогона удалить из БД. Дизайн-токены — `design-system/it-audit-platform/MASTER.md` (Tailwind `@theme` в globals.css).
- i18n (T-022): `GET /subsidiaries` (Bearer+`X-Tenant-Slug: demo`) — `name` на локали юзера; `?locale=ru` → «Демо-банк», `?locale=az`, невалидная → 400; сущность без перевода падает на EN (fallback). UI: переключатель `data-testid="locale-en|az|ru"` пишет cookie и делает router.refresh(). Каталоги переводов `apps/web/src/messages/*.json` — тест паритета ключей `npx vitest run src/messages` (из apps/web).
- LDAP (T-025): `POST /auth/ldap/login` `{"username":"ldap-user","password":"Ldap-User-2026"}` → `{accessToken}`, `/auth/me` = ldap-user@demo.io (JIT, `password_hash IS NULL`); username может быть и mail. Неверный пароль/неизвестный юзер → 401 (+строка `failed` в auth_event), пустой пароль → 400 (гард от анонимного bind). Сид-юзеры — `infra/ldap/*.ldif`, применяются только при первом старте контейнера (правки = `docker compose up -d --force-recreate ldap`). Прямая проверка LDAP: `docker exec it-audit-platform-ldap-1 ldapsearch -x -D cn=admin,dc=demo,dc=io -w admin -b ou=people,dc=demo,dc=io '(uid=ldap-user)'`.
- MFA (T-014): готовый сценарий `bash <scratchpad>/mfa-e2e.sh` или руками: `POST /auth/mfa/setup` (Bearer) → `{secret, qrDataUrl}`; TOTP-код: `node -e "console.log(require('<repo>/apps/api/node_modules/otplib').generateSync({secret:'...'}))"` (запускать с резолвом из apps/api!); `POST /auth/mfa/enable {code}` → 10 recovery-кодов; логин становится двухшаговым (`{mfaRequired, mfaToken}` → `POST /auth/mfa/verify`); recovery-код одноразов (10→9, повтор — 401).
- ⚠ Гоча портов: если 3001 занят «бессмертным» процессом — ищи родителя `nest start --watch` (`fuser 3001/tcp` → `ps -o ppid`), он воскрешает убитого ребёнка; гасить надо всё дерево (pkill -f 'nest start').
- Фоновые задачи (T-040): `curl -X POST 'http://localhost:3001/jobs/demo?delayMs=2000'` → `{id}`; сразу `GET /jobs/demo/<id>` — `state:"delayed"`, через ~3с — `completed` с `returnValue`. `GET /jobs/heartbeat` — свежий `lastRunAt` (repeatable-джоба: первый прогон при старте api, дальше раз в минуту).
- Email (T-041): `curl -X POST http://localhost:3001/email/demo -H 'Content-Type: application/json' -d '{"locale":"ru"}'` → `{messageId}`; письмо видно в Mailpit UI :8025 или `curl http://localhost:8025/api/v1/messages`. Локали en/az/ru; невалидная локаль → 400.
- Файлы (T-042): `curl -X POST http://localhost:3001/files -F file=@<путь>` → `{key}`; скачать `curl 'http://localhost:3001/files/content?key=<key URL-энкоженный>'` и сравнить байты (`cmp`). Отсутствующий ключ → 404. Ключи с кириллицей в query энкодить, иначе 400 от самого запроса.

(Доменный seed пока пуст — расти будет вместе со схемой, дополнять этот файл начиная с T-010.)
- Риски (T-057, EP-RISK): `POST /risks {titleI18n, inherentImpact, inherentLikelihood, residualImpact, residualLikelihood, treatment}` (control.edit; Collaborator→403) — risk_class вычисляется по impact×likelihood и порогам матрицы (дефолт medium≥6/high≥12/critical≥20). `PUT /risks/matrix {thresholds}` (settings.edit) настраивает пороги (RSK-02); `POST /:id/rescore` пересчитывает; GET список/карточка. E2e: 25→critical, 6/12/20→medium/high/critical, матрица critical≥15 меняет класс, impact>10→400. audit_log risk.created/rescored. Откат: 0027.
- RCM + heat map (T-058): `POST /risks/:id/controls {controlIds}` (control.edit) — привязка митигирующих контролей (дедуп, битый→400); `GET /risks/:id/controls`; `GET /risks/heatmap` → {total, inherent:{low/medium/high/critical}, residual:{…}}. Route heatmap до :id. audit_log risk.controls_linked. Откат: 0028.
- Risk assessment sessions (T-059): `POST /risk-assessments {title, period, methodologyNote, scope}` (control.edit) → draft; `POST /:id/transition {to:in_progress|completed}` (скип draft→completed→400); документы через document_link T-034 (entityType=risk_assessment); `GET /:id` карточка+документы. EP-RISK закрыт (T-057+T-058+T-059). Откат: 0029.
- Vendor risk (T-060/061, EP-VEND): `POST /vendors {name, inherentRisk:low|medium|high|critical, intake}` (control.edit) + lifecycle `POST /:id/transition {to:active|archived}` (procurement→active→archived); `POST /:id/assessments {type,dueDate}` + `POST /vendors/assessments/:id/transition {to:in_progress|completed, recommendation}` — evidence_status→provided при наличии документа (document_link entityType=vendor_assessment); GET списки. EP-VEND закрыт. Откат: 0030.
- Vulnerabilities (T-062, EP-VULN): `POST /vulnerabilities {title, cve, severity:low|medium|high|critical, dueDate}` (control.edit) + `POST /:id/transition {to:open|remediating|resolved}` (недопустимый→400); sla_status на SLA-джобе T-043 (due-вчера→overdue). GET список. audit_log. Откат: 0031.
- Change management (T-063, EP-VULN): `POST /changes {title, type, risk:low|medium|high, approverMembershipId}` (control.edit) → requested; `POST /:id/transition {to:approved|rejected|deployed}` (control.view) — approve/reject только назначенный approver (иначе 403), workflow requested→approved/rejected→deployed (скип requested→deployed→400); GET список. audit_log change.created/status_changed. E2e: approver approve→approved→deploy, чужой approve→403, скип→400, Collaborator create→403. Откат: 0032.
- Security alerts (T-064, EP-VULN): `POST /security-alerts {title, source, severity:low|medium|high|critical, connectorId}` (control.edit) → new; `POST /:id/transition {to:triaged|closed, note}` (control.edit) — new→triaged/closed→closed (недопустимый→400), note→triage_note; GET список (control.view). audit_log security_alert.created/status_changed. E2e: create→new, triage(+note)→triaged, close→closed, повторный close→400, мусорный severity→400, Collaborator create→403. Откат: 0033. EP-VULN закрыт.
- Audit universe (T-065, EP-ASSET): `POST /universe {kind:subsidiary|process|system|location|activity|function, nameI18n, parentId, descriptionI18n, ownerMembershipId, custom}` (control.edit) — дерево неограниченной глубины (битый parent→400); `POST /:id/move {parentId}` — смена родителя, защита от цикла (self→400, потомок→400); `GET /universe` плоский список (control.view); `GET /:id` узел+дети+permanent-документы. Permanent-file: upload документа с entityType=auditable_entity relation=permanent_file. audit_log auditable_entity.created/moved. E2e: 3-уровневое дерево, цикл→400, валидное перемещение, мусорный kind→400, permanent-file documents=1, Collaborator→403. Откат: 0034.
- Assets + risk_entity (T-066, EP-ASSET): `POST /assets {type:erp|db|network|app|cloud|endpoint|other, name, subsidiaryId, ownerMembershipId, attrs, custom}` (control.edit) → {id, entityId} — актив проецируется в universe (kind=system, ref_id=asset.id); `GET /assets` (fromConnector-флаг), `GET /assets/:id`. Risk↔universe: `POST /risks/:id/entities {entityIds}` (control.edit, дедуп, битый узел→400), `GET /risks/:id/entities`. audit_log asset.created + risk.entities_linked. E2e: проекция kind=system refId=asset, risk↔узел linked=1/дедуп=0, битый узел→400, мусорный type→400, Collaborator→403. Откат: 0035.
- Business processes (T-067, EP-ASSET, ADR-0016): `POST /processes {nameI18n, criticality:low|medium|high|critical, subsidiaryId, ownerMembershipId, scoring}` (control.edit) → {id, entityId, scope} — subsidiary_id NULL=каталог группы (scope group) / задан=локальный дочки (scope local); проекция в universe (kind=process, ref_id=process.id); `GET /processes` (scope-флаг), `GET /processes/:id`. audit_log business_process.created. E2e: групповой→проекция kind=process, локальный дочки scope=local, мусорный criticality→400, Collaborator→403. Откат: 0036.
- Asset auto-discovery (T-068, EP-ASSET, B3): `POST /assets/import {connectorId, type}` (control.edit) — гейт по capability `inventory` (нет→400), collectRecords→маппинг record→asset→upsert по (connector_id, external_id), новые проецируются в universe (kind=system). audit_log asset.imported {imported, updated}. E2e (демо-openldap с capability=inventory): imported=1/updated=0, повторный→imported=0/updated=1 (идемпотентность), personnel-коннектор→400, несуществующий→404, Collaborator→403. Миграции нет (переиспользует колонки 0035). EP-ASSET закрыт.
- Personnel profiles (T-069, EP-PERS, B6): `POST /personnel {fullName, email, departmentId, position, employmentStatus, certificates, contacts}` (settings.edit) — ручной профиль; `POST /personnel/import {connectorId}` — гейт по capability `personnel` (нет→400), upsert по (connector_id, external_id); `POST /:id/status {status:active|onboarding|offboarded}` (мусорный→400); `GET /personnel` (settings.view, fromConnector-флаг). audit_log personnel.imported/created/status_changed. E2e: профиль аудитора+сертификаты, статус onboarding→active, импорт imported=1→повторный 0/1 (идемпотентность), не-personnel коннектор→400, Collaborator→403. Откат: 0037.
- Device / endpoint compliance (T-070, EP-PERS, B12): `POST /devices {name, os, serial, ownerPersonnelId, diskEncryption, screenLock, antivirus, passwordPolicy}` (control.edit) — compliance_status вычисляется (все 4 true→compliant); `POST /:id/checks {…частичный patch}` пересчитывает (пустой→400); `GET /devices` (control.view, checks-объект). audit_log device.created/checks_updated. E2e: 1 провал→non_compliant, все true→compliant, чиним/ломаем пересчёт, пустой patch→400, Collaborator→403. Откат: 0038.
- Endpoint auto-discovery + автотест (T-071, EP-PERS, B12): `POST /devices/import {connectorId}` (control.edit) — гейт по capability `inventory`, bool-проверки из полей записи→compliance_status, upsert по (connector_id, external_id), source=mdm. AutoTestService: новое правило check_config `{type:"device_compliant"}` (без коннектора) — non_compliant устройства→failing_entities. Continuous-loop: import→device_compliant автотест fail→починка→pass. E2e: import imported=1/повторный 0/1, автотест fail→pass, не-inventory→400, Collaborator→403. Миграции нет (переиспользует 0038). EP-PERS закрыт.
- Chart-dashboards (T-072, EP-REP, B9): `GET /dashboards/metrics` — каталог 7 метрик; `POST /dashboards {name, widgets:[{metric, chartType:bar|pie|donut|number|line, title}]}` (report.export edit; неизвестная метрика/чарт→400); `GET /dashboards` список; `GET /dashboards/:id/data` — каждый виджет с посчитанным breakdown из живых данных (finding.status/riskRating, test.status, vendor.inherentRisk, risk.riskClass, device.complianceStatus, control total). audit_log dashboard.created. E2e: 7 метрик, дашборд из 3 виджетов→живые данные, неизвестная метрика→400, мусорный chartType→400, Collaborator→403. Откат: 0039.
- Snapshots (T-073, EP-REP, B10): `POST /snapshots {label}` (report.export edit) — MetricsService.computeAll замораживает все 7 метрик в jsonb на дату; `GET /snapshots` список, `GET /snapshots/:id` замороженные метрики. audit_log report_snapshot.created. Неизменность: базлайн→мутация живых данных→тот же снапшот прежний, новый отражает изменение. E2e: неизменность OK, новый отличается, Collaborator→403. Откат: 0040. M2 закрыт целиком.
- ROPA register (T-074, EP-PRIV, GDPR Art.30): `POST /processing-activities {nameI18n, legalBasis:consent|contract|legal_obligation|vital|public|legitimate, role:controller|processor|joint, purpose, dataCategories, dataSubjects, recipients, retentionPeriod, crossBorder}` (control.edit) → active; `POST /:id/archive` (повторная→400); `GET /processing-activities` реестр, `GET /:id` карточка. audit_log processing_activity.created/archived. E2e: payroll contract+cross_border+категории, недопустимый legal_basis/role→400, архивация→повторная 400, Collaborator→403. Откат: 0041.
- DPIA (T-075, EP-PRIV): `POST /processing-activities/dpia {processingActivityId, title, riskLevel:low|medium|high, necessityNote, mitigations}` (control.edit; битая операция→400); `POST /dpia/:id/transition {to:in_progress|completed}` (скип draft→completed→400); документы через document_link entityType=privacy_assessment; `GET /dpia`, `GET /dpia/:id` (+документы). Маршруты dpia — до :id операции. audit_log privacy_assessment.created/status_changed. E2e: DPIA high, битая операция→400, скип→400, workflow, docs=1, Collaborator→403. Откат: 0042. EP-PRIV закрыт.
- Tags (T-076, EP-MISC): `POST /tags {name, color}` (control.edit; дубликат имени→400); `POST /tags/:id/attach {entityType, entityId}` (whitelist 9 типов, дедуп, битый тег→404, недопустимый type→400); `DELETE /tags/:id/attach`; `GET /tags/of?entityType=&entityId=`; `GET /tags`. audit_log tag.created/attached. E2e: тег, дубликат→400, навесить на control→true/повтор false, недопустимый type→400, снять→0, Collaborator→403. Откат: 0043.
- Commitments (T-077, EP-MISC): `POST /commitments {title, source, description, dueDate, reviewCadence, ownerMembershipId}` (control.edit) → met; `POST /:id/status {status:met|at_risk|breached}` (мусорный→400); `GET /commitments`. SLA-джоба T-043 расширена (due_date→overdue/due_soon/ok, met не трогается). audit_log commitment.created/status_changed. E2e: uptime-SLA met→at_risk, мусорный→400, SLA-пересчёт commitments=1→overdue, breached, Collaborator→403. Откат: 0044. Tags+Commitments в EP-MISC закрыты.
- Framework library expansion (T-078, EP-FWK): сид GLOBAL_FRAMEWORKS расширен на SOC 2/PCI DSS 4.0/GDPR/HIPAA (idempotent, под owner) — библиотека 7 фреймворков. `GET /frameworks/:id/requirements` (JwtAuth, ?locale) — дерево требований {id, name, version, requirements:[{id,ref,title,parentId}]}. E2e: библиотека 7, SOC 2→4 требования, GDPR ru локализуется, несуществующий→404. Пересид: `pnpm seed` (idempotent). Миграции нет.
- Framework coverage (T-079, EP-FWK): `GET /frameworks/:id/coverage` (JwtAuth) — {frameworkId, total, covered, percent, uncovered[]} через LEFT JOIN framework_requirement↔control_mapping. E2e (демо-маппинги): ISO 3/3=100%, COBIT 1/2=50% (непокрыт APO01), NIST 2/2=100%, SOC 2 0/4=0%, несуществующий→404. Миграции нет. EP-FWK закрыт.
- Trust Center (T-080, EP-TRUST, B7): `POST /trust-center {slug, title, intro, isPublic}` (settings.edit, upsert по тенанту) + `POST /trust-center/items {label, category, published}` + `GET /trust-center` (админ-вид, все items). **Публично без auth**: `GET /trust/:slug` — {title, intro, items[published]} только если is_public (иначе 404). RLS: trust_center имеет доп. политику public_read (SELECT is_public), items — под withTenant(владелец). E2e: приватный→404, публикация→анонимный GET, черновик скрыт (public 1/admin 2), несуществующий→404, невалидный slug→400, Collaborator→403. Откат: 0045.
- Trust Center access requests (T-081, EP-TRUST): публичный `POST /trust/:slug/access-requests {email, company, message}` (без auth) → pending (невалидный email→400, непубличный slug→404). Админ: `GET /trust-center/access-requests` (settings.view), `POST /trust-center/access-requests/:id/decision {to:approved|denied}` (settings.edit; повторное→400). trust_access_request RLS через trust_center EXISTS. audit_log trust_access_request.created(actorUserId=null)/decided. E2e: публичный→pending, непубличный→404, невалидный email→400, admin approve→повторное 400, Collaborator→403. Откат: 0046. EP-TRUST закрыт.
- Knowledge base (T-082, EP-QA): `POST /kb {question, answer, category, tags}` (control.edit); `GET /kb?q=` (control.view) — ILIKE OR по question/answer, пустой q→все. audit_log kb_entry.created. E2e: 2 записи, поиск по вопросу/ответу→1, нет совпадения→0, пустой q→все, пустая question→400, Collaborator→403. Откат: 0047.
- Questionnaires + KB reuse (T-083, EP-QA): `POST /questionnaires {title, source}` (control.edit) + `POST /:id/questions {question}` → pending; `POST /questionnaires/answers/:answerId {answer | kbEntryId}` — kbEntryId копирует kb.answer + ставит kb_entry_id (ни того ни другого→400, битый KB→400); `POST /:id/submit` (пусто/pending→400, всё answered→submitted); `GET /:id` (reusedFromKb-флаг). audit_log questionnaire.created/answered/submitted. E2e: submit до ответов→400, reuse KB→reused=true (ответ скопирован), pending→400, ручной→reused=false, submit, без answer/kbEntryId→400, Collaborator→403. Откат: 0048. EP-QA ядро закрыто.
- Audit types (T-084, EP-AUDITTYPES): сид +investigations (7 типов). `GET /audit-types?locale=` (engagement.view; global+tenant, isGlobal-флаг) + `POST /audit-types {code, nameI18n}` (settings.edit; дедуп→400, невалидный code→400). engagement `?auditTypeCode=` фильтр. audit_log audit_type.created. Пересид: `pnpm seed`. E2e: список 7 (ru «Расследования»), кастомный forensic_it isGlobal=false, дубликат/невалидный code→400, engagements ?auditTypeCode=it→все IT, Collaborator→403. Миграции нет (RLS из 0011).
- Type-specific checklist templates (T-085, EP-AUDITTYPES, UNI-06): `POST /audit-types/:id/template-items {ref, objectiveI18n, questionI18n, order}` (settings.edit), `GET /audit-types/:id/template-items` (engagement.view), `POST /audit-types/seed-checklist/:engagementId` (engagement.edit) — engagement.audit_type_id→template→checklist_item (без типа→400). audit_log audit_type.checklist_seeded. E2e: шаблон IT 2→засев seeded=2 (checklist_item в БД=2), без типа→400, пустой шаблон→0, Collaborator→403. Откат: 0049. EP-AUDITTYPES закрыт.
- Custom fields (T-086, EP-CONFIG, GEN-07): `POST /custom-fields {entityType, key, labelI18n, fieldType:text|number|date|select|boolean, options, required}` (settings.edit; дубликат key→400); `GET /custom-fields?entityType=` (settings.view); `POST /custom-fields/validate {entityType, values}` — тип/required/select-опция/неизвестный ключ→400. audit_log custom_field_def.created. E2e: 2 поля, дубликат→400, валидный→true, неверный тип/required/чужая опция/неизвестный ключ→400, Collaborator→403. Откат: 0050.
- Config lists (T-087, EP-CONFIG, GEN-06/ENG-09): `GET /config-lists/:listKey` (settings.view; override тенанта ИЛИ дефолт, isDefault-флаг, неизвестный→404); `PUT /config-lists/:listKey {items:[{code, labelI18n}]}` (settings.edit; пустой/дубликаты кодов→400); `GET /config-lists/:listKey/validate?code=` (вне списка→400). DEFAULT_LISTS.audit_opinion. audit_log config_list.updated. E2e: дефолт 3 isDefault=true, validate ok/400, неизвестный→404, override→isDefault=false, старое значение→400, дубликаты→400, Collaborator→403. Откат: 0051. EP-CONFIG закрыт.
- Time entries (T-088, EP-TIME, TIME-01): `POST /time-entries {date, hours, engagementId, phase:planning|fieldwork|reporting, category:audit|training|leave|admin, billableRate, note}` (engagement.view); `GET /time-entries?engagementId=` (фильтр). hours numeric(6,2). audit_log time_entry.created. E2e: fieldwork 6.5ч, 3 всего/2 по engagement, невалидная фаза/категория→400, часы>24/0→400. Откат: 0052.
- Budget vs actual (T-089, EP-TIME, UNI-07): `PUT /time-entries/budget/:engagementId {budgetedHours}` (engagement.edit), `GET /time-entries/summary/:engagementId` (engagement.view) — sum(hours) group by phase/category, {budgeted, actual, variance, byPhase, byCategory}. engagement.budgeted_hours numeric(8,2). audit_log engagement.budget_set. E2e: бюджет 20+4 записи→actual=13 variance=7, byPhase/byCategory верны, пустой→0/null, несуществующий→404, Collaborator→403. Откат: 0053. EP-TIME закрыт.
- API keys (T-090, EP-API, INT-01): `POST /api-keys {name}` (settings.edit) → {id, name, prefix, key} (plaintext ОДИН раз); `GET /api-keys` (без секрета, revoked-флаг); `DELETE /api-keys/:id` (revoke). ApiKeyGuard: X-Api-Key→tenant-контекст. RLS: api_key_auth_read SELECT USING(true) для аутентификации по хэшу, api_key_write FOR ALL tenant-изоляция. E2e: prefix iap_, plaintext НЕ в БД, список без key, отзыв→повторный false, пустое имя→400, Collaborator→403. Откат: 0054.
- OpenAPI + v1 API (T-091, EP-API, INT-01): `/docs` (Swagger UI) + `/docs-json` (OpenAPI спека, X-Api-Key security-scheme); `GET /api/v1/controls` под ApiKeyGuard — программный доступ. E2e: /docs-json openapi 3.0.0/158 путей/api-key scheme, доступ с ключом→32 контроля, без/битый/не-iap/отозванный→401. Миграции нет. EP-API закрыт (RFP INT-01 ✓).
- Working papers + sign-off (T-092, EP-WPAPERS, WP-02/07/08): `POST /working-papers {engagementId, title, content}` (engagement.edit; preparer=автор); `POST /:id/transition {to}` draft→prepared→in_review→reviewed→signed_off (reviewed: reviewer≠preparer→403 WP-07, sign_off); `PUT /:id/content` (после reviewed→editedSinceReview=true WP-08); `GET /working-papers?engagementId=`, `GET /:id` (+подписи). RLS через engagement/working_paper EXISTS. E2e: workflow, reviewer=preparer→403, admin(≠preparer) reviewed→2 подписи, правка→editedSinceReview=true, signed_off, скип→400, Collaborator→403. Откат: 0055.
- Audit programs + roll-forward (T-093, EP-WPAPERS, ENG-04/05): `POST /audit-programs {titleI18n, engagementId, subjectArea, steps[]}` (engagement.edit); `POST /:id/roll-forward {targetEngagementId}` — клон в новый engagement, origin_program_id=источник, шаги копируются со сбросом status→pending; `GET /audit-programs?engagementId=`, `GET /:id` (isTemplate+шаги). RLS: audit_program tenant_isolation, program_step через EXISTS. audit_log audit_program.created/rolled_forward. E2e: 3 шага→roll-forward→клон origin=OK шаги pending, ENG2=1, несуществующая→404, Collaborator→403. Откат: 0056. EP-WPAPERS закрыт.
- Global search (T-094, EP-SEARCH, GEN-05): `GET /search?q=` (engagement.view) — кросс-сущностный ILIKE по finding(title/description i18n)/control(ref+objective)/working_paper(title)/kb_entry(question/answer)/audit_program(title), лимит 5 на тип, RLS-изоляция. {query, hits:[{type,id,label,snippet}]}. Пустой/без q→400. Миграции нет. E2e: 'access'→finding/control/WP, 'encryption'→control+kb, пустой→400, бессмысленное→0, лимит≤5. EP-SEARCH ядро закрыто.
- Glossary (T-095, EP-HELP, GEN-09): `GET /glossary?q=&locale=` (engagement.view; global-сид + кастом тенанта, ILIKE по term, isGlobal-флаг) + `POST /glossary {term, definitionI18n, category}` (settings.edit; дедуп→400). Сид 6 GRC-терминов (Control/Finding/RCM/DPIA/ROPA/ITGC). RLS glossary_read=global|tenant. Пересид: `pnpm seed`. audit_log glossary_term.created. E2e: 6 сид (RCM ru локализуется), поиск DPIA→1, кастомный CBAR isGlobal=false, дубликат→400, Collaborator→403. Откат: 0057.
- Notification center (T-096, EP-MSG, GEN-04): `POST /notifications {recipientMembershipId, title, type:info|warning|action, body}` (settings.edit); `GET /notifications` (engagement.view; только СВОИ + unread-счётчик); `POST /:id/read` (только своё, чужое no-op). RLS tenant_isolation + recipient-фильтр в коде. E2e: admin шлёт 2+1→видит 2 unread=2, read→unread=1, collaborator видит своё, чужое-read no-op (изоляция), Collaborator create→403. Откат: 0058.
- Satisfaction survey (T-097, EP-MSG, ISS-06): `POST /satisfaction-surveys {engagementId, rating 1-5, comment}` (engagement.view; битый engagement→400); `GET /satisfaction-surveys/summary/:engagementId` — {responses, averageRating}. audit_log satisfaction_survey.submitted. E2e: пустой→0/null, (5,4,3)→avg=4, рейтинг 0/6/3.5→400, несуществующий→400. Откат: 0059. EP-MSG закрыт.
- Tenant-wide export (T-098, EP-REPWIZ, REP-05): `GET /reports/export?entity=findings|risks|controls&format=csv|xml` (report.export edit) — CSV (экранирование) / XML (экранирование), Content-Disposition attachment. Миграции нет. E2e: controls CSV заголовки+32 строки, risks XML корень <risks>, findings.csv disposition, мусорный entity/format→400, Collaborator→403.
- Period comparison (T-099, EP-REPWIZ, REP-03): `GET /reports/compare?a=&b=` (report.export view) — дельта метрик двух снапшотов (T-073) по breakdown-ключам {a, b, delta}. Миграции нет. E2e: снапшот A→мутация→B→compare non_compliant delta=1, controls стабильны, несуществующий→404, без параметров→400. EP-REPWIZ закрыт.
- Risk-based plan + capacity (T-100/101, EP-PLAN): `POST /plans {name, year, capacityHours}` + `POST /:id/items {auditableEntityId, plannedHours, recurrence}` (engagement.edit) — priority_score из макс. risk_class узла (critical=100/high=60/medium=30/low=10); `GET /:id` (items по priority desc, recurring-флаг); `GET /:id/capacity` — {capacity, planned, remaining, overallocated}. RLS: annual_plan tenant_isolation, plan_item EXISTS. E2e: critical-узел→priority=100, без риска→0, сортировка, capacity 100/план 90→false, +20→overallocated=true, recurring помечен, битый узел→400, Collaborator→403. Откат: 0060. EP-PLAN закрыт.
- Resource allocation (T-102, EP-SCHED, SCH-02/03): `POST /allocations {engagementId, membershipId, allocatedHours, period}` (engagement.edit; битый engagement→400); `GET /allocations/utilization?membershipId=&capacity=` — sum(allocated) кросс-engagement vs capacity, {allocated, capacity, utilizationPct, overallocated}. audit_log resource_allocation.created. E2e: 50+30=80/160→pct=50, capacity 60→pct=133 over=true, битый engagement/0 часов/не-число capacity→400, Collaborator→403. Откат: 0061. EP-SCHED ядро закрыто.
- Group consolidated risk map (T-103, EP-GROUP): `GET /group/summary` расширен — group.risksByClass + per-subsidiary risksByClass (risk.subsidiary_id→risk_class, scope-aware: standalone только при полном скоупе). Без миграции. E2e: групповая risk-карта {critical/high/medium/low}, дочки с полем, findings-консолидация T-012 цела, scope=group. EP-GROUP закрыт.
- Hash-chain audit (T-104, LOG-01, EP-HARDEN): AuditLogService.record — пер-тенант hash-chain (set_config tenant → RLS пускает читать прошлое звено, advisory-лок per-tenant, hash=sha256(prev+canonical)); системные (tenant NULL) без цепочки. `GET /audit/verify-chain` (settings.view) — пересчёт+линковка. Миграции нет (prev_hash/hash из T-021). E2e: 4 записи→valid=true (3/4 связаны), подмена after→content-hash brokenAt, подмена hash→false, Collaborator→403. Чистка тест-строк: owner-роль DELETE (обходит RLS).
- Config export/import (T-105, TEC-04, EP-HARDEN): `GET /config/export` (settings.edit) → бандл {version:1, auditTypes кастомные, configLists overrides, customFieldDefs, glossary кастомные}; `POST /config/import` — zod-валидация (version≠1/мусор→400) + onConflictDoUpdate. Миграции нет. E2e: экспорт 4 секции (forensic_it/audit_opinion/2 поля/CBAR), удалил CBAR→импорт восстановил, без version→400, version:2→400, Collaborator→403.
- Control KPI (T-106, CTL-04, EP-LOWCODE): `POST /control-kpis {controlId, name, unit, targetValue, currentValue, direction:higher_better|lower_better}` (control.edit; битый контроль→400); `POST /:id/value {currentValue}` → on-track (higher: current≥target / lower: current≤target); `GET /control-kpis?controlId=` (on-track-флаг). audit_log control_kpi.created. E2e: 80<95→false, замер 96→true, lower_better 10>7→false/5→true, битый→400, Collaborator→403. Откат: 0062.
- Audit universe UI (T-U01, UI-трек): `apps/web/src/app/universe/page.tsx` — server component, дерево auditable_entity под `GET /universe` (X-Tenant-Slug из getActiveTenantSlug), build tree по parentId, бейджи по kind, resolve nameI18n[locale]. i18n namespace `universe` (en/ru/az) + account.universe. Ссылка из /account (data-testid=go-universe). Verify: web `npx next start -p 3000`; /universe без сессии→307 /login; с cookie session=<token> → рендер (en: Audit Universe/HQ Group/Subsidiary/universe-tree; ru: Аудируемая вселенная/Дочка/узлов), http 200.
- Risk heat-map UI (T-U02, UI-трек): `apps/web/src/app/risk-heatmap/page.tsx` — матрица 2×4 (inherent/residual × low/medium/high/critical) под `GET /risks/heatmap`, цветные ячейки (emerald/amber/orange/red) + число + метка класса (семантика не только цветом). i18n namespace `riskHeatmap` + account.riskHeatmap. Ссылка /account (go-risk-heatmap). Verify: /risk-heatmap без сессии→307; с cookie → рендер (en Inherent/Residual/Critical/risk-heatmap; ru Присущий/Остаточный), счётчики верны (critical inherent=5), http 200.
- Audit plans UI (T-U03, UI-трек): `apps/web/src/app/plans/page.tsx` — список планов под `GET /plans` + per-plan `GET /plans/:id/capacity`, capacity progress-bar (bg-accent / bg-destructive при overallocated), planned/capacity/remaining. i18n namespace `plans` + account.plans. Ссылка /account (go-plans). ВАЖНО: headers типизировать `Record<string,string>` (иначе TS union-ошибка HeadersInit). Verify: /plans без сессии→307; с cookie → рендер (Over capacity при planned 110>100), en/ru, http 200.
- Chart dashboards UI (T-U04, UI-трек): `apps/web/src/app/dashboards/page.tsx` — под `GET /dashboards` + per-dashboard `GET /:id/data`, виджеты с горизонтальными барами (BAR_TONE по классу critical/high/medium/low/compliant…, иначе bg-accent). i18n namespace `chartDashboards` (не путать с `dashboard`) + account.chartDashboards. Ссылка /account (go-dashboards-charts). Verify: /dashboards без сессии→307; с cookie → GRC overview/devices_compliance/controls_total, en/ru, http 200.
- Trust Center UI (T-U05, UI-трек): `apps/web/src/app/trust-center/page.tsx` — админ-вид под `GET /trust-center` (404→notConfigured empty) + `GET /trust-center/access-requests` (счётчик), public/private бейдж, items с published/draft-статусом, slug /trust/:slug. i18n namespace `trustCenter` + account.trustCenter. Ссылка /account (go-trust-center). Verify: /trust-center без сессии→307; с cookie → Demo Group Security/Public/Published/trust-items, en/ru, http 200.
- Glossary UI (T-U06, UI-трек): `apps/web/src/app/glossary/page.tsx` — под `GET /glossary?locale=` (локализованные определения), карточки term+definition, бейдж standard/custom (isGlobal). i18n namespace `glossary` + account.glossary. Ссылка /account (go-glossary). Verify: /glossary без сессии→307; с cookie → RCM/DPIA, ru «Матрица риск-контроль»/Стандартный, en Standard/Custom, http 200.
- Snapshots UI (T-U07, UI-трек): `apps/web/src/app/snapshots/page.tsx` — под `GET /snapshots` + per-snapshot `GET /:id`, карточки с локализованной датой (Intl.DateTimeFormat) + замороженные метрики (sum controls_total/risks_by_class/findings_by_status). i18n namespace `snapshots` + account.snapshots. Ссылка /account (go-snapshots). Verify: /snapshots без сессии→307; с cookie → Период A/B/Controls/Risks/Findings, en/ru, http 200.
- Devices UI (T-U08, UI-трек): `apps/web/src/app/devices/page.tsx` — под `GET /devices`, compliance-бейдж (compliant emerald / non_compliant red) + 4 MDM-проверки ✓/✕ (текст+цвет, не только цвет). i18n namespace `devices` + account.devices. Ссылка /account (go-devices). Verify: /devices без сессии→307; с cookie → Endpoint Compliance/new-dev/Non-compliant/Disk encryption, en/ru, http 200.
- Personnel UI (T-U09, UI-трек): `apps/web/src/app/personnel/page.tsx` — таблица под `GET /personnel`, статус-бейдж (active/onboarding/offboarded) + источник (fromConnector/manual). i18n namespace `personnel` + account.personnel. Ссылка /account (go-personnel). Verify: /personnel без сессии→307; с cookie → Personnel/Иван Аудитов/Active/From connector, en/ru, http 200.
- Vendors UI (T-U10, UI-трек): `apps/web/src/app/vendors/page.tsx` — таблица под `GET /vendors`, риск-класс бейдж (low/medium/high/critical) + статус (procurement/active/archived). i18n namespace `vendors` + account.vendors. Ссылка /account (go-vendors). Verify: /vendors без сессии→307; с cookie → Vendor Risk/AWS/High/Active, en/ru, http 200. — UI-трек: 10 экранов (T-U01…U10), паттерн одинаков (server component, apiFetch+X-Tenant-Slug из getActiveTenantSlug, headers: Record<string,string>, i18n namespace + account.<key>, ссылка из /account).
- Working papers UI (T-U11, UI-трек): `apps/web/src/app/working-papers/page.tsx` — селектор engagement (searchParams.engagementId, Next15 `searchParams: Promise<>` → await) через chip-links, WP-список под `GET /working-papers?engagementId=` со статус-бейджем (draft…signed_off) + флаг editedSinceReview (WP-08, amber). i18n namespace `workingPapers` + account.workingPapers. Verify: /working-papers?engagementId=<с WP> → Access review WP/Signed off/Edited since review, en/ru, http 200.
- Vulnerabilities UI (T-U12, UI-трек): `apps/web/src/app/vulnerabilities/page.tsx` — таблица под `GET /vulnerabilities`, severity-бейдж (low…critical) + status + SLA-статус (overdue=destructive). i18n namespace `vulnerabilities` + account.vulnerabilities. Ссылка /account (go-vulnerabilities). Verify: /vulnerabilities без сессии→307; с cookie → Log4Shell/CVE-2021-44228/Critical/Overdue, en/ru, http 200.
- Security alerts UI (T-U13, UI-трек): `apps/web/src/app/security-alerts/page.tsx` — таблица под `GET /security-alerts`, severity + status (new/triaged/closed) бейджи. i18n namespace `securityAlerts` + account.securityAlerts. Ссылка /account (go-security-alerts). Verify: без сессии→307; с cookie → Security Alerts/New/Medium, en/ru, http 200.
- Commitments UI (T-U14, UI-трек): `apps/web/src/app/commitments/page.tsx` — таблица под `GET /commitments`, статус met/at_risk/breached + SLA (overdue=destructive). i18n namespace `commitments` + account.commitments. Ссылка /account (go-commitments). Verify: без сессии→307; с cookie → 99.9% uptime SLA/MSA Acme/Breached/Overdue, en/ru, http 200.
- Access reviews UI (T-U15, UI-трек): `apps/web/src/app/access-reviews/page.tsx` — под `GET /access-reviews`, кампании UAR со статусом (draft/in_progress/completed). i18n namespace `accessReviews` + account.accessReviews. Ссылка /account (go-access-reviews). Verify: без сессии→307; с cookie → Access Reviews/Q3 Access Review/Completed, en/ru, http 200. — UI-трек итог: 15 экранов (T-U01…U15) read-only списков/визуализаций под готовый API, единый паттерн.
- UI интерактивность — эталон (T-U16): server actions `apps/web/src/app/<screen>/actions.ts` ('use server' + apiFetch POST с X-Tenant-Slug из getActiveTenantSlug + revalidatePath). Формы: `<form action={serverAction}>` с name-полями; для выбора значения — submit-кнопки `name="status" value="X"` (значение уходит в FormData). Пример: commitments — createCommitmentAction (FormData title/source) + setCommitmentStatusAction. Verify: рендер формы (commitment-create) + список отражает новое + status-кнопки (value=met/at_risk/breached).
- UI triage/transition (T-U17): security-alerts (transitionAlertAction, NEXT new→[triaged,closed]/triaged→[closed]) + vulnerabilities (transitionVulnAction, NEXT open→[remediating,resolved]/remediating→[resolved]). Кнопки `name="to" value="X"` per allowed-переход. Verify: рендер value="triaged/closed" (sa) и value="remediating/resolved" (vuln) на строках с открытым статусом.
- UI vendors interactivity (T-U18): createVendorAction (name + inherentRisk select) + transitionVendorAction (NEXT procurement→[active,archived]/active→[archived]). Форма с `<select name="inherentRisk">` (defaultValue medium). Verify: vendor-create форма + 4 risk-опции + lifecycle value="active/archived" + новый вендор в списке.
- UI drill-down эталон (T-U19): detail-страница `app/<screen>/[id]/page.tsx` — `params: Promise<{id}>` (Next15, await), apiFetch(`/x/${id}`) → notFound() при !ok, back-ссылка, вложенные данные (assessments). Список делает имена `<Link href="/x/${id}">`. Пример: /vendors/[id] (детали + assessments). Verify: имя-ссылка в списке, /x/[id] с cookie→200, несуществующий→404, back-link, en/ru.
- Risks register UI (T-A01, Трек A): `apps/web/src/app/risks/page.tsx` + actions.ts — реестр под `GET /risks?locale=` (riskClass/residualClass бейджи, treatment) + форма создания (title + impact/likelihood селекты 1-5 inherent/residual + treatment select → createRiskAction POST /risks). i18n namespace `risks` + account.risks. Ссылка /account (go-risks). Verify: без сессии→307; с cookie → Risk Register/Group risk/Critical/risk-create, 9 form-опций, en/ru, http 200.
- Privacy ROPA/DPIA UI (T-A02, Трек A): `apps/web/src/app/privacy/{page,actions}.tsx` — ROPA под `GET /processing-activities` (создание name+legalBasis+role, архив-кнопка) + DPIA под `GET /processing-activities/dpia` (создание с processingActivity-select+title+riskLevel, workflow draft→in_progress→completed). i18n namespace `privacy` + account.privacy. Ссылка /account (go-privacy). Verify: ropa-create/dpia-create/ropa-list/dpia-list рендерятся, Biometric access/Consent/Controller, en/ru, http 200.
- IAM UI (T-A03, Трек A): `apps/web/src/app/iam/{page,actions}.tsx` — accounts под `GET /accounts` (deactivate-кнопка для active) + access-requests под `GET /access-requests` (approve/reject decision для pending). Decision body `{decision: approve|reject}`. i18n namespace `iam` + account.iam. Ссылка /account (go-iam). Verify: accounts-table/access-requests-list, leaver-1/AWS prod, Deactivated/Approved, en/ru, http 200.
- Change management UI (T-A04, Трек A): `apps/web/src/app/code-changes/{page,actions}.tsx` — под `GET /changes`, создание (title+type+risk) + approval-workflow requested→[approved,rejected]/approved→[deployed] (transition-кнопки). i18n namespace `codeChanges` + account.codeChanges. Ссылка /account (go-code-changes). Verify: change-create/changes-table, Requested/Approved/Deployed, workflow value="approved/rejected", en/ru, http 200.
- Config/настройки UI (T-A05, Трек A): `apps/web/src/app/config/{page,actions}.tsx` — справочник типов аудита (`GET /audit-types`, создание `POST {code, nameI18n}` — settings/edit) + теги (`GET /tags`, `POST {name, color?}` — control/edit). Формы audit-type-create / tag-create, списки audit-types-list / tags-list, бейдж Global/Custom. i18n namespace `config` + account.config. Ссылка /account (go-config). Verify: config http 200, EN Settings/Audit types/Tags/Global/Custom, RU (cookie locale=ru) Настройки/Типы аудита/Теги/Глобальный/Свой.
- Reports UI (T-A06, Трек A): `apps/web/src/app/reports/page.tsx` + `reports/export/route.ts` (same-origin прокси cookie→Bearer, т.к. браузерная ссылка не несёт токен). Выгрузка findings/risks/controls в CSV/XML (`GET /reports/export`) + сравнение двух снапшотов (`GET /reports/compare?a=&b=`, GET-форма с двумя select из `/snapshots`, diff-таблицы по группам метрик с Δ). i18n namespace `reports` (+ metrics.* с фолбэком t.has→сырой ключ) + account.reports. Verify: reports http 200; export-table/compare-form/dl-{entity}-{fmt}; export-прокси → 200 + Content-Disposition attachment + text/csv; compare-result с переведёнными метриками; EN/RU.
- API-keys UI (T-A07, Трек A): `apps/web/src/app/api-keys/{page,actions,new-key-form}.tsx`. Генерация (`POST /api-keys`, plaintext-ключ показывается один раз — клиентский `useActionState` в new-key-form, без навигации) + список (`GET /api-keys`, prefix/lastUsed/status) + отзыв (`DELETE /api-keys/:id`). i18n namespace `apiKeys` + account.apiKeys. Verify: api-keys http 200, apikey-create/apikeys-table, EN Generate/Active/Revoke + iap_ prefix, RU Сгенерировать/Активен/Отозвать.
- Notifications UI (T-A08, Трек A): `apps/web/src/app/notifications/{page,actions}.tsx` — `GET /notifications` → {unread, items}; счётчик непрочитанных (unread-badge), бейдж типа info/warning/action, «прочитано» (`POST /notifications/:id/read`). i18n namespace `notifications` + account.notifications. Verify: http 200, notifications-list/unread-badge/Mark read, EN Info/Warning/Action, RU Инфо/Внимание/Действие/Прочитано.
- Time-tracking UI (T-A09, Трек A): `apps/web/src/app/time/{page,actions}.tsx` — тайм-запись (`POST /time-entries`, date из input[type=date]→ISO datetime, hours/phase/category/engagement) + список (`GET /time-entries`, engagementId→title через `/engagements`) + итог часов (tfoot). i18n namespace `time` (phases/categories) + account.time. Verify: http 200, time-create/time-table/time-total, EN Fieldwork/Audit/Total, RU Полевые работы/Аудит/Итого.
- Control-KPI UI (T-A10, Трек A): `apps/web/src/app/kpi/{page,actions}.tsx` — выбор контроля (GET-форма из `/controls`, label ref—objective) → KPI (`GET /control-kpis?controlId=`), создание (`POST /control-kpis` {name,unit,targetValue,currentValue,direction}) + замер (`POST /control-kpis/:id/value` {currentValue} → onTrack). i18n namespace `kpi` (dir.*) + account.kpi. Verify: kpi?controlId= http 200, kpi-control-select/kpi-create/kpi-table, EN On track/Off track/Measure, RU В норме/Отклонение/Замер.
- Audit-programs UI (T-A11, Трек A, закрывает A1): `apps/web/src/app/audit-programs/{page,actions}.tsx` — создание программы (`POST /audit-programs` {titleI18n, subjectArea, engagementId?, steps[]} — title/step→i18n одинаково) + список (`GET /audit-programs`, titleI18n через resolveLocalized из @it-audit/shared) + roll-forward (`POST /:id/roll-forward` {targetEngagementId}, select engagement). i18n namespace `auditPrograms` + account.auditPrograms. Verify: http 200, program-create/programs-list, EN «ITGC program»/Roll forward/Create, RU Программы аудита/Перенести/Шаг.
- Knowledge-base UI (T-A12, Трек A): `apps/web/src/app/knowledge-base/{page,actions}.tsx` — поиск (GET-форма q → `/kb?q=` ILIKE) + создание (`POST /kb` {question, answer, category?}) + список Q&A. i18n namespace `knowledgeBase` + account.knowledgeBase. Verify: http 200, kb-search/kb-create/kb-list, поиск ?q=encryption фильтрует; EN Knowledge base, RU База знаний/Найти/Ответ.
- Questionnaires UI (T-A13, Трек A): `apps/web/src/app/questionnaires/{page,actions}.tsx` (список + создание `POST /questionnaires` {title,source}) + `[id]/{page,actions}.tsx` (детали: ответы `POST /questionnaires/answers/:answerId` {answer}, добавить вопрос `POST /:id/questions`, submit `POST /:id/submit` — кнопка disabled пока не все отвечены). Бейдж reusedFromKb «from KB». i18n namespace `questionnaires` + account.questionnaires. Verify: list+detail http 200, questionnaire-create/questionnaires-list/answers-list, EN Submitted/from KB/«Do you encrypt data at rest», RU Опросники/Отправлен.
- Satisfaction UI (T-A14, Трек A): `apps/web/src/app/satisfaction/{page,actions}.tsx` — выбор engagement (GET-форма) → сводка (`GET /satisfaction-surveys/summary/:engagementId` → averageRating/responses) + оценка 1-5 radio + comment (`POST /satisfaction-surveys` {engagementId,rating,comment}). i18n namespace `satisfaction` + account.satisfaction. Verify: http 200, satisfaction-select/summary/submit, EN Average rating/Responses/Submit rating, RU Средний рейтинг/Ответов. ПРИМ: allocations пропущен — нет list-эндпоинта memberships для селектора участника.
- Plans интерактив (T-A15, Трек A): `apps/web/src/app/plans/{page,actions}.tsx` — к read-only capacity-вьюхе добавлено создание плана (`POST /plans` {name,year,capacityHours}) + добавление items из universe (`POST /plans/:id/items` {auditableEntityId,plannedHours}; селектор из `/universe`, nameI18n через resolveLocalized) + список items с priorityScore. i18n: доп. ключи namePh/year/add/empty/priority/pickEntity/addItem. Verify: http 200, plan-create/plans-list/plan-items, EN Create plan/Priority/Pick auditable entity, RU Создать план/Приоритет/Выберите объект аудита.
- Account-хаб навигация (T-A16, Трек A2): `apps/web/src/app/account/page.tsx` — плоский список 35 ссылок → 7 категорий (NAV_GROUPS: overview/compliance/engagements/risk/security/thirdParty/org), широкий max-w-5xl grid карточек, профиль+logout в шапке. i18n account.nav.* (7 заголовков). ВАЖНО: все data-testid go-<slug> сохранены (verify прежних экранов зависит от них). Verify: /account http 200, 35 go-* testid, 7 категорий (EN Overview/Compliance…, RU Обзор/Комплаенс…), account-name/account-email/logout/onboarding на месте.
- i18n-гвард (T-A17, Трек A2): `apps/web/scripts/check-i18n.mjs` + npm-скрипт `check-i18n`. Проверяет паритет leaf-ключей en/ru/az, exit 1 при расхождении. Запуск: `cd apps/web && node scripts/check-i18n.mjs` (или `pnpm --filter web check-i18n`). Текущий статус: ✅ 596 ключей × 3 локали, 41 namespace. Прогонять после любого добавления i18n-неймспейса.
- Unit-тест onTrack (T-A18, Трек A3): `onTrack` вынесен из control-kpis.service в `apps/api/src/control-kpis/on-track.ts`; тест `apps/api/test/on-track.spec.ts` (9 кейсов: higher/lower_better, null-данные, границы) — чистая функция, БД не нужна. Прогон: `cd apps/api && npx vitest run test/on-track.spec.ts`. Полный api-suite: 7 файлов / 29 тестов зелёные.
- Unit-тест classifyRisk (T-A19, Трек A3): `classify` вынесен из risks.service в `apps/api/src/risks/classify-risk.ts` (+ DEFAULT_THRESHOLDS 6/12/20); тест `apps/api/test/classify-risk.spec.ts` (6 кейсов: low/medium/high/critical по границам, null-данные, кастомные пороги). Полный api-suite: 8 файлов / 35 тестов зелёные.
- Unit-тест diffMetrics (T-A20, Трек A3): diff-логика вынесена из reports-export.service в `apps/api/src/reports-export/diff-metrics.ts`; тест `apps/api/test/diff-metrics.spec.ts` (6 кейсов: delta=b-a, односторонние ключи/группы, идентичные, пустые). ВНИМАНИЕ: под noUncheckedIndexedAccess индексный доступ в тесте требует `!`. Полный api-suite: 9 файлов / 41 тест зелёные.
- Unit-тест serialize (T-A21, Трек A3): `csvCell`/`xmlEscape` вынесены из reports-export.service в `apps/api/src/reports-export/serialize.ts`; тест `apps/api/test/serialize.spec.ts` (10 кейсов: CSV RFC-4180 quoting/удвоение кавычек/0/false, XML escape & первым, комбинации, null). Полный api-suite: 10 файлов / 51 тест зелёные.
- Trends UI (T-A22, Трек A3, RSK-08 subset): `apps/web/src/app/trends/page.tsx` — серия метрики по снапшотам во времени. GET `/snapshots` (сорт по capturedAt) + N деталей (metrics), выбор group+key (GET-форма), inline SVG-полилиния (без внешних либ, TrendChart, data-testid trend-svg). i18n namespace `trends` (groups.*) + account.trends (в группе overview). Verify: trends http 200, trend-select/trend-chart/trend-svg/polyline, переключение group=risks_by_class&key=critical, EN Metric/Series, RU Метрика/Серия.
- Backend memberships (T-A23, разблокировка allocations): `apps/api/src/memberships/{controller,service,module}.ts` + регистрация в app.module. `GET /memberships?locale=` — участники тенанта (RBAC settings/view): join membership×user×role, поля {id,userId,fullName,email,category,status,role(resolveLocalized)}. membership без RLS → фильтр по tenantId явно. Verify: `curl -H "Authorization: Bearer <t>" -H "X-Tenant-Slug: demo" localhost:3001/memberships` → 200, count>0; ?locale=ru → роль «Администратор»; ?locale=xx → 400.
- Allocations UI (T-A24, снимает блокер Трека A): `apps/web/src/app/allocations/{page,actions}.tsx` — создание аллокации (`POST /allocations` {engagementId,membershipId,allocatedHours,period}; member-селектор из нового `/memberships`, engagement из `/engagements`) + утилизация команды (`GET /allocations/utilization?membershipId=&capacity=`, capacity из searchParams default 160, progressbar allocated/capacity с overallocated). i18n namespace `allocations` + account.allocations (группа engagements). Verify: http 200, allocation-create/capacity-form/utilization-list/progressbar, capacity=80 → aria-valuemax="80"; E2E POST 40ч → utilization 40 (25%); EN Allocate/Team utilization, RU Назначить/Утилизация команды.
- Unit-тест deriveUtilization (T-A25, Трек A3): вынесено из allocations.service в `apps/api/src/allocations/utilization.ts`; тест `apps/api/test/utilization.spec.ts` (5 кейсов: округление %, ровно 100%, overallocated >100%, ноль, capacity≤0→null). Полный api-suite: 11 файлов / 56 тестов зелёные.
- CBAR framework seed (T-C01, Трек C): в `apps/api/src/seed.ts` GLOBAL_FRAMEWORKS добавлен местный регулятор CBAR (назван клиентом в client-answers.md): «CBAR IT Audit» / az «MB İT Audit», v1.0, 16 требований по доменам IT-аудита из docs/client-templates/checklist-analysis.md (GOV/AC/CM/…/SA), трилингв. EN/AZ/RU. Идемпотентный сид (7→8 фреймворков). Verify: `node dist/seed.js` → «фреймворков: 8»; `GET /frameworks?locale=en` содержит «CBAR IT Audit»; `GET /frameworks/:id/requirements` → 16 reqs; web /frameworks (en «CBAR IT Audit v1.0», az «MB İT Audit»).
- CBAR control coverage (T-C02, Трек C): в seedGlobalControls (seed.ts) после демо-маппингов — авто-маппинг каждого из 31 глобального контроля на CBAR-требование его домена (control.domain = requirement.ref), onConflictDoNothing (идемпотентно). Verify: `node dist/seed.js` → «маппинги + CBAR-покрытие»; `GET /frameworks/<cbar-id>/coverage` → {total:16, covered:16, percent:100, uncovered:[]}.
- Прод-Dockerfile API (T-I01, Трек I / EP-ONPREM single-artifact): `apps/api/Dockerfile` (multi-stage, node:24-slim, `pnpm install --filter @it-audit/api...` без web/sharp, build shared+api; ВАЖНО копировать `tsconfig.base.json` — shared/api его наследуют, иначе tsc падает на zod-типах) + `.dockerignore`. Контекст сборки — корень репо. Verify: `docker build -f apps/api/Dockerfile -t it-audit-api:local .` → образ ~814MB; `docker run -d --network host -e API_PORT=3009 -e JWT_SECRET=x it-audit-api:local` → `/health` 200, `/health/infra` 200 (все 4 сервиса), `/auth/login` 200 (реальный DB). Дефолтные env-URL совпадают с хост-портами инфры (5433/6380/9000/1025).
- Прод-Dockerfile web (T-I02, Трек I / EP-ONPREM): `apps/web/Dockerfile` (multi-stage, `pnpm install --filter @it-audit/web...`, build shared+web, runtime `next start`; копировать tsconfig.base.json + next.config.ts + src/messages; public-dir нет). Verify: `docker build -f apps/web/Dockerfile -t it-audit-web:local .` → ~1.12GB; `docker run -d --network host -e API_URL=http://localhost:3001 it-audit-web:local sh -c "node_modules/.bin/next start -p 3010"` → `/login` 200, `/account` без сессии 307, с `Cookie: session=<token>` 200 + «Demo Admin» (web→API fetch в контейнере). Чистка: `docker rm -f it-audit-web-test`.
- Миграции в API-образе (T-I03, Трек I): в runtime API-Dockerfile добавлены `apps/api/drizzle/` (SQL+journal) + `drizzle.config.ts` → образ умеет `drizzle-kit migrate` (drizzle-kit — devDep, уже в образе; схема для applying не нужна). Verify: `docker run --rm --network host -e DATABASE_URL_OWNER=postgres://audit:audit@localhost:5433/audit it-audit-api:local sh -c "cd apps/api && node_modules/.bin/drizzle-kit migrate"` → «migrations applied successfully» (идемпотентно); app-старт `/health` 200 не сломан.
- Prod-compose (T-I04, Трек I / EP-ONPREM single-command deploy): `docker-compose.prod.yml` — postgres+redis+minio+minio-init + migrate (one-shot, condition service_completed_successfully) + api + web (build: из Dockerfile'ов). Роль app создаётся миграцией 0001, поэтому порядок postgres→migrate→api работает. JWT_SECRET обязателен (нужен и для `up`, и для `down`). Демо-данные НЕ сеются (prod). SMTP/SSO — внешние (потому /health/infra=degraded без SMTP, это ок). Verify (изолированно, alt-порты): `JWT_SECRET=x API_PORT=3101 WEB_PORT=3100 docker compose -f docker-compose.prod.yml up -d --build` → migrate exit 0 «migrations applied»; api :3101/health 200; web :3100/login 200, /account 307. Teardown: `JWT_SECRET=x docker compose -f docker-compose.prod.yml down -v`.
- CI docker-job (T-I05, Трек I): в `.github/workflows/ci.yml` добавлен параллельный job `docker` — `docker build` api+web образов + `docker compose -f docker-compose.prod.yml config` (JWT_SECRET=ci-validate). Локальный pre-verify: YAML валиден (jobs ci+docker); `docker build -f apps/api/Dockerfile -t it-audit-api:ci .` и web аналогично собираются; compose config OK. Финальный verify — реальный прогон: `gh run watch` / `gh run list -L 3` после push (оба job'а green).
- Починка красного CI (T-I06): CI был `failure` ~25 коммитов подряд, т.к. verify гонялся per-dir (`next lint --dir`), а не полный. ДВЕ причины: (1) eslint-глоб `scripts/**/*.mjs` не матчил `apps/web/scripts/check-i18n.mjs` → Lint no-undef на console/process (фикс: `**/scripts/**/*.mjs` в eslint.config.mjs); (2) `format:check` дрейф 79 файлов, невидимый т.к. Lint падал раньше в порядке шагов (фикс: `pnpm format`). УРОК/ОБЯЗАТЕЛЬНО перед «зелёным»: из корня `pnpm build && pnpm lint && pnpm typecheck && pnpm format:check && pnpm test` (полный, как в CI), НЕ только `next lint --dir X`. Финальная сверка — `gh run watch`/`gh run list -L 3`.
- Prod-bootstrap (T-I07, Трек I): `apps/api/src/bootstrap.ts` (→ dist/bootstrap.js) — чистая инсталляция БЕЗ демо: референс-данные (переиспользует экспортированные из seed.ts `seedPermissionCatalog`/`seedPresetRoles`/`seedGlobalFrameworks`/`seedGlobalControls`/`seedAuditTypes`/`seedGlossary`) + первый tenant+admin из BOOTSTRAP_TENANT_SLUG/NAME/ADMIN_EMAIL/PASSWORD/NAME. seed.ts: main() под гардом `require.main === module` (иначе импорт запускал бы демо-сид). Сервис `bootstrap` в docker-compose.prod.yml (после migrate, api ждёт его completed). Verify: `pnpm seed` (демо) всё ещё работает; изолир. postgres+migrate+`node dist/bootstrap.js` с BOOTSTRAP_* → CBAR + admin + tenant, 0 демо-юзеров/tenant; в prod-compose с BOOTSTRAP_* — логин admin@acme.io даёт JWT, CBAR виден через API.
- SEC-07 конкурентные сессии (T-H01, EP-HARDEN subset): в `completeLogin` (auth.service) перед выдачей токена — `recentActiveLogins()` считает `login`-события в auth_event за окно env.jwtTtlSeconds; если >0 → `recordAuthEvent('concurrent_session')`, и при `CONCURRENT_SESSION_POLICY=block` кидает 401. Тип события расширен в AuditLogService.AuthEventRecord. env.concurrentSessionPolicy detect(деф)/block. Без миграции. Тест `apps/api/test/concurrent-session.spec.ts` (3 кейса; auth_event чистится под owner — app имеет только INSERT/SELECT). Verify: двойной логин admin@demo.io в окне TTL → в auth_event появляется concurrent_session (SQL count растёт), оба логина 200 (detect). api-suite: 12 файлов / 59 тестов.
- TEC-03 версионирование config-lists (T-H02, EP-HARDEN subset): колонка `config_list.history` (jsonb, миграция 0063_config-list-history + down DROP COLUMN, up→down→up проверен). `ConfigListsService.set()` — при наличии текущей записи снимок {items, at, by} в начало history (cap 20); `getHistory()` + `GET /config-lists/:listKey/history` (settings/view). Тест `apps/api/test/config-list-history.spec.ts` (3 кейса). Verify: PUT audit_opinion×2 → `GET /config-lists/audit_opinion/history` versions[0].items = прошлая версия; api-suite 13 файлов / 62 теста.
- Field-level права фундамент (T-H03, SEC-04/EP-HARDEN, ADR-0020): таблица `field_permission` (roleId×entityType×field→level hidden|view|edit; миграция 0064 + down; RLS как role_permission — БЕЗ FORCE, чтобы owner сеял глобальные роли). Чистые хелперы `apps/api/src/rbac/field-policy.ts`: `maskFields(obj,levels)` (вырезает hidden, fast-path тот же объект при пустых levels, не мутирует), `rejectedWriteFields(input,levels)` (поля не-edit во входе). Резолвер `RbacService.fieldLevels(userId,slug,entityType)`. Тест `apps/api/test/field-policy.spec.ts` (9). Verify: миграция up→down→up (table field_permission); `vitest run test/field-policy.spec.ts`; api-suite 71.
